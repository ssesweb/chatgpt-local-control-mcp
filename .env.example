# MCP HTTP endpoint
PORT=8787
# 127.0.0.1 keeps the service local-only; set 0.0.0.0 to map it out.
HOST=127.0.0.1
MCP_PATH=/mcp

# File tools are restricted to these roots. Separate multiple paths with the
# platform path delimiter: ":" on macOS/Linux, ";" on Windows.
# Keep this narrow. Add ~/Desktop, ~/Documents, or C:\Users\you only if you really want ChatGPT
# to see and modify those folders.
LOCAL_CONTROL_ROOTS=.

# Privileged tools can use this as a fallback tool argument named control_pin.
# ChatGPT OAuth authorization does not require entering this PIN by default.
LOCAL_CONTROL_PIN=change-me-to-a-long-random-secret

# Read-only tools never require auth. OAuth-authorized write/control tools get
# a bearer token with the local.control scope.
OAUTH_TOKEN_TTL_SECONDS=86400
# Optional: set to 1 if you want the OAuth authorization page to ask for the
# fallback PIN before issuing a token.
OAUTH_REQUIRE_APPROVAL_PIN=0

# Enable privileged capabilities one by one.
ALLOW_WRITES=0
ALLOW_SHELL=0
ALLOW_SCREENSHOT=0
ALLOW_OPEN=0
ALLOW_APPLESCRIPT=1
ALLOW_GUI=0

# Shell execution is never a true sandbox. By default only a small executable
# allowlist is accepted. Set ALLOW_UNSAFE_SHELL=1 only for a trusted, temporary
# local session.
ALLOW_UNSAFE_SHELL=0
SAFE_EXECUTABLES=pwd,ls,find,cat,head,tail,wc,du,df,ps,whoami,hostname,git,git.exe,node,node.exe,npm,npm.cmd,npx,npx.cmd,python,python.exe,python3,python3.exe,rg,rg.exe,sed,where,where.exe,ipconfig,tasklist

# Output and file safety limits.
MAX_COMMAND_OUTPUT_CHARS=20000
MAX_READ_BYTES=262144
MAX_DIRECTORY_ENTRIES=300
COMMAND_TIMEOUT_MS=15000

# Public HTTPS URL of the MCP endpoint, printed at startup as the
# ready-to-paste ChatGPT connector URL. Leave empty to show the tunnel hint.
# PUBLIC_MCP_URL=https://your-domain.example/mcp

# Secret key that grants full control via ?secret-key= or the x-secret-key
# header. If empty, one is generated once and stored in .mcp-artifacts/secret-key.txt.
# SECRET_KEY=change-me-to-a-long-random-secret
