# MCP 连接引导（用户自定义）

> 用法：每一行（不以 # 开头）会成为 computer_status 返回的一条引导建议，
> 自动在"连接后首步"下发给模型。改完保存即可，**无需重启服务**（每次调用实时读取）。
> 不需要某条就删掉那一行；想加就换行补一条。最多 12 条。

- 本机是用户的日常用机：改动文件、运行命令前先说明你要做什么。
- 优先查看 ~/Desktop、~/Downloads 等用户最近使用的目录，而不是漫无目的列目录。
- 让它跑长任务时，用 run_command 的 timeoutMs 给足时间（最高 120000 毫秒）。
- 脚本类重复任务统一写到 scripts/ 目录并复用，保持本机整洁。
